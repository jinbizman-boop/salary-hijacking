{
  "schemaVersion": 1,
  "observedAt": "2026-07-03T19:53:51.229Z",
  "source": "Generated by scripts/release/generate-secrets-evidence.mjs from local no-secret proof booleans; raw tokens, URLs, DSNs, webhooks, private keys, and secret values are rejected before writing",
  "secretsRedacted": true,
  "containsSecretValues": false,
  "secrets": {
    "DATABASE_URL": {
      "verified": false,
      "stores": ["GitHub Environments", "Cloudflare Worker secret"],
      "note": "Runtime Postgres URL must be verified without committing the URL."
    },
    "STAGING_DATABASE_URL": {
      "verified": false,
      "stores": ["GitHub Environments", "Cloudflare Worker secret"],
      "note": "Staging Postgres URL must be verified without committing the URL."
    },
    "NEON_API_KEY": {
      "verified": false,
      "stores": ["GitHub Environments"],
      "note": "Neon management token must be verified without committing the token."
    },
    "NEON_PROJECT_ID": {
      "verified": true,
      "stores": ["release external evidence"],
      "note": "NEON_PROJECT_ID presence is verified from read-only Neon project evidence without storing a Neon API key or database URL."
    },
    "CLOUDFLARE_API_TOKEN": {
      "verified": false,
      "stores": ["GitHub Environments", "Cloudflare account secret"],
      "note": "Cloudflare deployment token must be verified without committing the token."
    },
    "CLOUDFLARE_ACCOUNT_ID": {
      "verified": true,
      "stores": ["release external evidence"],
      "note": "CLOUDFLARE_ACCOUNT_ID presence is verified from read-only Cloudflare connector account evidence without storing an API token."
    },
    "CF_ADMIN_WORKER_NAME": {
      "verified": true,
      "stores": ["Cloudflare Worker config"],
      "note": "CF_ADMIN_WORKER_NAME matches the verified Salary Hijacking Admin Worker target without storing secret values."
    },
    "EXPO_TOKEN": {
      "verified": false,
      "stores": ["GitHub Environments", "EAS secret store"],
      "note": "EAS token must be verified without committing the token."
    },
    "EAS_PROJECT_ID": {
      "verified": false,
      "stores": ["GitHub Environments", "EAS project settings"],
      "note": "EAS project ID must match the Salary Hijacking mobile app."
    },
    "GITHUB_TOKEN": {
      "verified": true,
      "stores": ["GitHub Actions runtime"],
      "note": "GITHUB_TOKEN is verified as the automatic GitHub Actions runtime token for the matched Salary Hijacking repository without storing the token value."
    },
    "GITHUB_REPOSITORY": {
      "verified": true,
      "stores": ["release external evidence"],
      "note": "GITHUB_REPOSITORY matches the verified Salary Hijacking GitHub release target without storing a token."
    },
    "SENTRY_DSN": {
      "verified": false,
      "stores": ["GitHub Environments", "provider secret store"],
      "note": "Sentry DSN must be verified without committing the DSN."
    },
    "SLACK_WEBHOOK_URL": {
      "verified": false,
      "stores": ["GitHub Environments", "provider secret store"],
      "note": "Slack webhook URL must be verified without committing the webhook URL."
    }
  },
  "nextEvidenceRequired": [
    "DATABASE_URL presence proof in GitHub Environments or Cloudflare Worker secret",
    "STAGING_DATABASE_URL presence proof in GitHub Environments or Cloudflare Worker secret",
    "NEON_API_KEY presence proof in GitHub Environments",
    "CLOUDFLARE_API_TOKEN presence proof in GitHub Environments or Cloudflare account secret",
    "EXPO_TOKEN presence proof in GitHub Environments or EAS secret store",
    "EAS_PROJECT_ID presence proof in GitHub Environments or EAS project settings",
    "SENTRY_DSN presence proof in GitHub Environments or provider secret store",
    "SLACK_WEBHOOK_URL presence proof in GitHub Environments or provider secret store"
  ]
}
